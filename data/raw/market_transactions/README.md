# HDB Market Transactions

This folder caches live transaction statistics and queries fetched from data.gov.sg.
It is populated dynamically as you run inquiries or make `@location` lookups in the app.
