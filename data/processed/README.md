# Processed Chunks

This folder contains the parsed and chunked plain-text equivalents of your raw PDFs.
These are automatically created by `ingestion/pdf_splitter.py` with line overlap parameters, ready for direct LLM ingestion or RAG indexing.
